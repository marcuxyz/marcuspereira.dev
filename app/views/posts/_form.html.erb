<section>
  <div>
    <%= form_with url: search_path, method: :get do | form |%>
      <%= form.search_field :q, { placeholder: 'Busque o contéudo de seu interesse' } %>
    <% end %>
  </div>
</section>
